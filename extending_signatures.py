from ClassLexicon.py import *

def extending_signatures(lexicon, new_stem_length = 3):
	old_stem_length = lexicon.MinimumStemLength
	signatures = lexicon.SignatureToStems.keys()
	signatures.sort(key = len, reverse=True)

	lexicon.FindProtostem()  def FindProtostems(self, wordlist, Protostems,minimum_stem_length, FindSuffixesFlag ,maximum_stem_length = -1):
  
 

not finished.

